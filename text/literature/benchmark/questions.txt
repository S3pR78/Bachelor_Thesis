Welche Fragetypen und Query Typen das Benchmark abdecken soll?
Welche Evaluationsmetriken das Benchmark verwenden soll?
Wie ich Benchmark und Training trenne?
Warum mein Benchmark wissenschaftlich sinnvoll ist?


Diese Fragen soll ich mithilfe von Literaturen beantworten.


IOS_ORKG:

Das Paper sagt klar, dass ORKG-Templates dafür da sind, Forschungsbeiträge 
in einer konsistenten und maschinenlesbaren Form zu erfassen.
Genau das kann ich als Begründung nutzen, warum mein Benchmark nicht zufällig, sondern an der 
Template-Struktur orientiert aufgebaut wird.
Aus dem Paper kann man ableiten, dass die Benchmark Fragen enthalten sollte, die 
die ORKG-Stärken wirklich prüfen:

- strukturierte Faktenabfrage,
- Vergleich über Papers hinweg,
- Aggregation/Frequenz,
- Multi-hop über verschachtelte Template-Ketten,
- Fragen zu Reviews/Comparisons/Benchmarks.

Das folgt direkt aus der Rolle von ORKG für comparisons, reviews und benchmarks sowie aus der Aussage, 
dass symbolisches Querying und Inferenz komplexe Anfragen ermöglichen.

Das Paper betont, dass Templates mit SHACL-Vokabular, Constraints und Validierung arbeiten, 
damit Daten strukturiert, konsistent und vergleichbar bleiben. Denn man kann ein Sacverhalt auch auf verschiedene Weise in form von
Subject Prädikat und Objekt darstellen. 
Das ist für mich sehr nützlich, um in der Arbeit zu begründen, warum ich für Benchmark-Fragen 
Gold-SPARQL brauche und warum template-konforme Fragen besser sind als frei formulierte, unscharfe Fragen.

- ORKG unterstützt Benchmarks, Comparisons, Reviews als zentrale Wissensorganisationstypen.
- Benchmarks werden als standardisierte Datensätze und Metriken zur Bewertung und zum Vergleich von Methoden beschrieben.
- Das Template-System nutzt Constraints und Validierung, um Datenqualität zu sichern.
- ORKG ASK unterstützt natural language queries über Literatur und Knowledge Graph.
- Komplexe Abfragen profitieren von symbolic querying, reasoning and inference.

Frage für ORKG-ASK: Which question types and query complexities should a benchmark for natural-language-to-SPARQL over scholarly knowledge graphs include?



SciQA:
2-stage benchmark design: 100 manual + 2465 generated.
Benchmark dimensions: Objectives, Data Structure, Data Collection
Question classification: paper-based vs comparison-based
Question content classes: 
    factoid(Einfache Faktenfragen (Wer? Was?)), 
    non-factoid(Komplexere Erklärungen), 
    superlative(Suche nach Extremwerten), 
    negation(dinge ausschließen), 
    count,
    ranking(Anordnung von Ergebnissen in einer bestimmten Reihenfolge), 
    temporal

SPARQL complexity metadata: number of triple patterns, query shape
    single edge,
    chain,
    star,
    cycle,
    tree,
    forest
Automatic generation should be constrained when data is heterogeneous

Fragen für ORKG-ASK:
1) Which question types should be included in a benchmark for scholarly question 
answering over ORKG-style research contributions and comparisons?

2) What kinds of comparison-based, count, temporal, ranking, 
and superlative questions can be answered from ORKG comparisons?

3)Which benchmark questions over ORKG templates would cover both paper-based and comparison-based querying patterns?


KGQA:
Dataset minimum: NL question, SPARQL query, endpoint/graph, idealerweise expected answers.

Main benchmark dimensions: 
ambiguity:Das Paper zeigt, dass viele Fragen Oberflächenformen mit sehr vielen 
                Entity-Kandidaten enthalten und dass die richtige Entität oft nicht die populärste ist. 
                Es bedeutet das: In dem Benchmark sollte ich nicht nur „saubere“ Fragen haben, 
                sondern auch einige Fragen, 
                bei denen Entity Linking oder Disambiguierung nicht trivial ist.

lexical gap: Das Paper beschreibt den lexical gap als fehlende direkte Übereinstimmung 
                zwischen der Formulierung in natürlicher Sprache und den Labels von Entitäten, 
                Properties oder Klassen im KG. Das ist für deine ORKG-/Template-Welt sehr 
                relevant, weil User-Fragen oft anders formuliert sind als Property- oder Template-Namen. 
                Für den Benchmark heißt das: Nicht alle Fragen sollten die 
                Schema-Begriffe 1:1 wiederholen.

complex queries:Das Paper betont schon im Abstract, dass simple One-Triple-Fragen 
                oft lösbar sind, während komplexe Queries mit Subqueries oder mehreren Funktionen 
                weiterhin schwierig sind. Für den Benchmark ist das eine sehr gute argumentative 
                Begründung dafür, warum ich neben einfachen Lookup-Fragen auch Aggregationen, 
                Vergleiche, Negation, mehrstufige Pfade und komplexere Query-Formen brauche.

templates: Dass das Paper templates ausdrücklich als eigene Benchmark-Dimension 
            betrachtet, ist für mich besonders passend. meine Arbeit ist ja stark 
            template-basiert. Das gibt mir eine gute theoretische Stütze dafür, 
            warum ich meine Fragen nicht nur nach Inhalt, sondern auch nach wiederkehrenden 
            Frage-/Query-Mustern betrachten sollte
ontology types: 
answer types;

Simple questions are not enough; complex queries remain hard.

Benchmark choice influences system evaluation strongly.

No single benchmark is universally “easy” or “hard”; datasets differ by challenge profile.

ORKG ASK: What benchmark dimensions are most useful for analyzing question answering datasets over knowledge graphs, especially ambiguity, lexical gap, complex queries, templates, ontology types, and answer types?




Lehmann:
Train/validation/test split matters and template distribution should be checked. the paper uses 70, 10 , 20 split. Training, validation, benchmark
Use multiple metrics, not only exact match.
Fine-tuning small models can outperform larger models.
Similarity-based few-shot selection works best.
Zero-shot is too weak on difficult scholarly KGQA benchmarks.
Error analysis should separate entity, predicate, syntax, and semantic errors.
Automatically generated benchmark parts can inflate pattern-learning effects.
Execution metrics are desirable; string similarity alone is limited.

ORKG ASK: 
1) Which evaluation metrics and error categories are most useful for analyzing text-to-SPARQL systems on a scholarly knowledge graph benchmark?
2) What kinds of few-shot example selection strategies are most useful for question-to-SPARQL generation over scholarly knowledge graphs?

LC_QuAD:
Paraphrases reduce overfitting to fixed question syntax.
Complexity comes from multiple dimensions, not just more triples.
Controlled generation via templates is better than unconstrained generation.
Question types should be explicitly categorized.
Not every predicate fits every question type; filtering matters.
Benchmark quality needs a verbalization/paraphrasing validation step.


