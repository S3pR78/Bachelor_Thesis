"""Offline ACE-style playbook construction namespace.

The existing offline workflow remains available through the stable modules in
``src.ace`` such as ``offline_loop``, ``traces``, ``reflector``, and
``curator``. Offline ACE-style playbook construction analyzes a completed
evaluation run and derives context rules afterward.
"""

